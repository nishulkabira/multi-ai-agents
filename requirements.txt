# Streamlit Community Cloud deployment
# Entry point: streamlit_app.py  |  Python: 3.13 (see .python-version)
# Versions pinned to the set verified working locally in .venv

streamlit==1.62.0

# LangGraph research pipeline
langgraph==1.2.11
langchain==1.3.18
langchain-core==1.6.1
langchain-groq==1.1.3

# HTTP calls to SerpAPI / Bright Data
requests==2.34.2

# Structured output + typed graph state
pydantic==2.13.5
typing-extensions==4.16.0

# Local .env loading (no-op on Cloud; secrets come from st.secrets)
python-dotenv==1.2.3
